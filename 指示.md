

このアプリは、別のところから、内容を引き継ごうと七得ます。
別のところで実装していた時はDBの扱いが今と違い、一旦ローカルに保存していました。
（アプリのローカルではなく、プロジェクトのassetsに保存する方式）
その頃の「確認用lib」に過去の実装をまとめています。

この時の実装について、データベースはsupabaseを使いながら、知識カードの編集などのUIを、今のアプリに取り込んでください。